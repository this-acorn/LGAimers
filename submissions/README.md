# Baseline and intermediate models

Standalone inference packages spanning the initial baselines, contextual feature models, and seed-averaged multiclass CatBoost models.

Each package contains `script.py`, dependencies, and model assets where available. See [usage](../docs/RUNNING.md) for the runnable example and [methodology](../docs/METHODOLOGY.md) for the modeling approach.
