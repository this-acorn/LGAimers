# Ensemble inference

Inference packages combining multiclass predictions, hierarchical and temporal residuals, fixed calibration, and complementary prediction stacks.

Each package uses frozen model assets and aligns predictions by row ID. See [methodology](../docs/METHODOLOGY.md#calibration-and-ensembling) for the approach and [usage](../docs/RUNNING.md) to run a selected package.
