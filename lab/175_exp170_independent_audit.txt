=== LAB 175: independent audit of exp/170_jy_proxy_scout.py ===
Audit date: 2026-09-01 Asia/Seoul
Scope: read-only audit; exp/170_jy_proxy_scout.py was not modified.

DECISION
--------
STANDALONE 2023 SIGNAL: PASS BOUNDARY/ALIGNMENT AUDIT, PENDING 2024 LOCKED-WEIGHT TRANSFER.
R-ONLY RESIDUAL HEAD: FAIL; DO NOT DEPLOY OR USE AS THE STANDALONE VERDICT.
2025 PACKAGE: DESIGN READY, BUILD FORBIDDEN UNTIL THE 2024 fixed-weight gates below pass.

REPRODUCED 2023 STANDALONE GEOMETRY
-----------------------------------
Rows: 245,525 (official season 2023, official row order)
Current-clean anchor score: -22.7427345010
Team endpoint score from saved float32 prediction: 95.4468743327
d = endpoint - anchor: +118.1896088337
K reported by exp170: 144.2661585324
2023 locked candidate weight: 0.9096234360311286
Reported optimal blend gain: +119.3679540419
Anchor/team prediction correlation: 0.9573797107
LGB/Cat mean versus saved ensemble max abs: 4.4703484e-08 (float32 rounding only)

CACHE / ROW / TARGET AUDIT
--------------------------
Cache: lab/127_y2023_full_features.joblib
Cache SHA256: fcef038746a40e279a2b30cc11b88eb8a431ab1a2c558317e752a98d1677c846
The SHA256 exactly matches the value frozen in lab/170_jy_proxy_discovery.json.
Schema: 127-full-discovery-v2, 101 features.
Official train.csv size and mtime_ns exactly match cache source_identity:
  size=368527723; mtime_ns=1787374526267533800.
Train positions are exactly contiguous 0..976059:
  2019=237,413; 2020=244,087; 2021=247,088; 2022=247,472.
Validation positions are exactly contiguous 976060..1221584:
  2023=245,525.
Cache train_y and valid_y are bit-exact to official targets at those positions.
Cached train/validation season columns are exact to the official rows.
Train and validation row-id/order fingerprints both reproduce exactly.
Neither control_success nor row_id is in feature_names.
No numeric validation feature is exactly equal to the target.
Largest absolute feature/target correlation was only 0.0812148 (f_cs_p_rate);
the five largest were all below 0.082.  valid_p0/target correlation=0.0703791.
Both train_p0 and valid_p0 are finite.

SEASON-FORWARD / LABEL-BOUNDARY FINDINGS
----------------------------------------
PASS for the 2023 standalone endpoint:
1. exp127 builds the cache from a strict prefix ending at 2023; the model train
   rows end at 2022 and the validation rows are exactly 2023.
2. Validation hierarchy, priors, constants, recovered pitch-type helpers, and
   p0 use history through 2022 only.  recover_pitch_labels is called only after
   the history prefix is cut, so a 2023 row cannot reconstruct a 2022 label.
3. Per-training-season hierarchy/CS/mix features use seasons strictly earlier
   than that training season where their labels matter.
4. exp170 fits the standalone 2023 LGB/Cat residuals on cached 2019-2022
   train_y-train_p0 and predicts cached 2023 valid_x/valid_p0.
5. Current-anchor alignment is guarded by exact official target/order equality;
   the independently reconstructed anchor score is exactly -22.7427345010.
6. Static path audit found no test.csv, external reference, archive model, ZIP,
   third-party prediction, or leaderboard value read by exp170.  The exp127
   cache is derived from official train.csv and own source code/OOF boundary.

IMPORTANT LIMITATION for the R-only head:
The purported <=2021 -> 2022 inner team prediction reuses the outer-2023 cache.
Most training-row tables are season-forward, but the cache's pfb helper and
global prior were fitted over full history through 2022.  Thus team_y2022 is not
a fully isolated <=2021 feature-build fold.  This does not contaminate the
standalone <=2022 -> 2023 endpoint, but it invalidates a strong strict-forward
claim for the one-probability R head.  Independently, the head already fails its
own gates: 2023 gain +0.5549, pitcher bootstrap p025 -1.4680, PASS=false.

GOVERNANCE / CACHE RISKS TO FIX IN A DEPLOY BUILDER
---------------------------------------------------
1. exp170 load_cache23 checks only feature count.  The present cache passed a
   stronger independent SHA/source/position/row-id/season/target audit, but a
   builder must call equivalent validation every time.
2. CACHE24 reuse checks feature_names and later row_ids, but not source hash,
   source mtime, categorical schema, p0 finiteness/range, or payload version.
   Force a clean rebuild for confirmation/deploy or add all these checks.
3. confirm() does not require discovery status PASS; current discovery status is
   HEAD_FAIL_STANDALONE_REVIEW.  Treat the head and standalone endpoint as two
   different experiments.  Only standalone may proceed to transfer review.
4. evaluation() reports same-fold optimal geometry.  Any 2024 oracle weight is
   diagnostic only.  The only admissible standalone blend weight for 2024 is
   the 2023-locked 0.9096234360311286, applied without retuning.
5. Saved endpoint arrays are float32 while JSON geometry was computed before
   saving in float64.  Recompute final fixed-weight metrics from the packaged
   replay predictions and record both hashes; expected differences are about
   1e-05 score, not material.

2024 PASS GATE FOR THE STANDALONE SIGNAL
----------------------------------------
Using freshly rebuilt official <=2023 features and the locked LGB/Cat specs:
1. Refit team endpoint on 2019-2023, predict 2024 once.
2. Reconstruct the exact current-clean 2024 anchor and verify official target/order.
3. Form p_fixed = current + 0.9096234360311286 * (team - current).
4. Do not use the 2024 same-fold oracle weight for selection or packaging.
5. Require fixed-weight total gain >= +12, early(month<=6)>0, late(month>6)>0,
   and pitcher-cluster bootstrap p025>0.
6. Require endpoint/current/p_fixed row counts, row-id hashes, target hashes,
   and [0,1] range checks to pass.  Record whether any clipping was needed.
7. If any gate fails, status is NO_DEPLOY and no test prediction/ZIP is built.

2025 DEPLOY PACKAGE DESIGN (ONLY AFTER THE ABOVE PASS)
------------------------------------------------------
Training/assets:
1. Read official train.csv only; history is seasons <=2024.  Never consume the
   127/170 joblib caches as deploy inputs.
2. Rebuild the exact 101-feature contract and fit all learned state from history:
   recovered auxiliary pitch labels, PFB helper, career/count-group tables,
   prior-season CS constants, hierarchical snapshots, volatility scale, and
   train-defined categorical vocabularies.
3. Generate training features season-forward for every train season.  For the
   final test/query feature builder, freeze state from all 2019-2024 history;
   never aggregate, fit, rank, or normalize over query/test rows.
4. Fit the locked LGB and Cat residual models to control_success-p0 with seed42,
   six threads, and the exact exp170 specs.  Save both models plus explicit
   versioned feature/LUT metadata and SHA256 manifests.
5. Rebuild the current clean champion inference endpoint independently, then
   apply the locked standalone blend weight.  No R-only head is included.

Inference/package:
6. script.py must consume each query row only through row-local raw transforms
   and frozen train-derived lookups.  Unknown categories/IDs receive explicit
   frozen defaults.  Preserve input row order and row_id exactly.
7. Package only source, trained models, frozen lookup/preprocess assets, and
   license/notices required by included code.  Exclude train/test CSV, targets,
   OOF predictions, lab caches, logs, nested ZIPs, and reference repository data.
8. Include a manifest with train.csv SHA256, code SHA256, model/asset SHA256,
   feature schema fingerprint, fixed weight, seeds, versions, and row counts.

MANDATORY QA BEFORE ZIP RELEASE
-------------------------------
A. Exact 2024 replay: deploy inference must reproduce the audited standalone
   team and fixed blend arrays (tight float tolerance; report max/mean abs).
B. Row independence: full batch vs singleton, shuffled batch, duplicated rows,
   and split/concatenated batches; max abs <=1e-7 or a documented numeric bound.
C. Schema/order: input and output row counts and row_id order exact; one finite
   probability column in [0,1]; no implicit pandas index column.
D. State boundary: remove/rename all lab caches and train.csv, then run the
   packaged inference successfully using only declared assets and query CSV.
E. Leakage scan: grep/package inventory confirms no control_success-dependent
   query computation, no query groupby/rolling/cumsum/rank/fit, and no 2024
   target or same-fold oracle constant embedded beyond the approved 2023 weight.
F. Determinism: two cold runs give identical float32 outputs and identical hash.
G. Full rehearsal: fresh environment, CPU six-thread ceiling, runtime/RAM logged,
   ZIP contents enumerated, nested archives/weights from external repos absent.

BOTTOM LINE
-----------
The +119 standalone 2023 result is not explained by target-column leakage,
season selection error, row-order mismatch, or wrong current anchor in the
audited cache/run.  It is a legitimate but highly correlated low-score endpoint
whose value is almost full replacement of the unusual 2023 current OOF anchor.
Its deployment value is therefore entirely contingent on the untouched 2024
fixed-weight transfer.  The R-only head is not approved.
