# 인수인계 — LG Aimers 9기 투구 제구 예측

> **다음 세션 Claude에게**: 이 문서를 먼저 끝까지 읽어라.
> 여기 적힌 것은 전부 **실측으로 확정된 사실**이다. 재조사하지 말고 그대로 신뢰하라.
> 특히 **§2 노이즈 바닥**을 어기면 하루치 작업을 통째로 날린다 (이미 한 번 겪었다).

최종 갱신: 2026-08-23 저녁 · 마감 2026-09-02 09:59 (리더보드 제출은 09-01까지)

---

## 1. 현재 성적

```
549.51  ─────────────  830.32  ──────────────────  1421.99
베이스라인(운영진)      우리(제출완료)                1위
   └──── +281 확보 ────┘└────── -592 남음 ──────────┘
```

| 제출 | 로컬 2024 홀드아웃 | 실제 리더보드 |
|---|---:|---:|
| 운영진 베이스라인 RandomForest | 415.6 | 549.51193 |
| 우리 GBDT 8시드 앙상블 (65피처) | 708.7 | 830.322760105 |
| **★ hand delta 포함 67피처 (제출 대기)** | **726.2~731.6** | **예상 ~847** |

**2026-08-23 갱신: `submit_gbdt.zip` 재빌드 완료, exp/08 전 항목 통과.**
사용자가 업로드하면 됨 (이름 submit.zip으로 변경 후 제출).

**수료 조건(Public 549.51 이상)은 이미 충족했다.** 이후는 순위 싸움.

### ★ 로컬 ↔ 리더보드 전이율 = 0.958
```
증가폭:  로컬 +293.1  →  리더보드 +280.81
→ 로컬에서 100점 올리면 리더보드에서 약 96점 오른다
→ 곱셈(비율)이 아니라 덧셈 관계. 로컬 점수 + 약 122~134 = 리더보드
```
**제출하지 않고 로컬만으로 개선폭을 예측할 수 있다.** 하루 5회 제한은 병목이 아니다.

---

## 2. ★★ 측정 노이즈 바닥 σ = 15.3 — 가장 중요한 규칙

피처·데이터·파라미터를 **완전히 고정하고 `random_state`만 8종 변경**했더니:

```
변별력 712.0 ~ 750.9  (폭 38.9, 표준편차 15.3)
```

### 반드시 지킬 것
```
❌ 단일 학습 1회로 판단하지 마라
✅ 모든 실험은 4~8시드 앙상블 예측으로 측정하라
✅ 단일학습 비교면 30점(2σ), 4시드 앙상블이면 15점 미만 차이는 전부 노이즈다
❌ 노이즈 범위의 차이에 서사를 붙이지 마라
```

**실제로 당한 사례**: exp/05에서 피처를 바꿀 때마다 점수가 계단처럼 내려가길래
"인코딩 실수 때문"이라고 원인까지 분석해 보고했는데, 나중에 노이즈 측정해보니
전부 동전 던지기였다. 표본 4~5개로 패턴을 읽으려는 유혹이 매우 강하니 조심하라.

### 덤: 시드 앙상블은 공짜 점수
```
단일 평균 728.3  →  8시드 앙상블 753.2   (+24.9)
   ※ 8개 중 최고값(750.9)보다도 앙상블이 높다
```

---

## 3. 제출 파일 구조

```
submit.zip           ← 현재 830.32점짜리 (사용자가 submit_gbdt.zip에서 이름 변경)
├── model/model.pkl   1.7MB · 8시드 모델 + 인코더 + prior + 피처목록
├── script.py         추론 코드 ★ 피처 생성 함수의 단일 원본
└── requirements.txt  numpy==1.26.4 / pandas==2.0.3 / scikit-learn==1.8.0 / joblib==1.5.3
                      (전부 서버 기본 탑재 버전 → 설치 단계에서 아무것도 다운로드 안 함)
```

### ⚠️ numpy 버전 함정 (반드시 지킬 것)
```
로컬 기본 python3.12:  numpy 2.1.3 / pandas 3.0.1   ← 여기서 저장하면 서버에서 못 연다
평가 서버:             numpy 1.26.4 / pandas 2.0.3
```
numpy 2.x로 만든 pickle은 numpy 1.x에서 `ModuleNotFoundError: numpy._core`로 실패한다.
**제출용 모델은 반드시 서버 버전이 맞춰진 venv로 저장하라:**
```
C:/Users/gwonn/AppData/Local/Temp/claude/c--Users-gwonn-Desktop-open/
  aecbab82-c8d9-4dad-8fa6-306809344e0a/scratchpad/venv311/Scripts/python.exe
  (python 3.11.8 / numpy 1.26.4 / pandas 2.0.3 / sklearn 1.8.0 / joblib 1.5.3)
```
없어졌으면 다시 만들 것:
```bash
"C:/Users/gwonn/AppData/Local/Programs/Python/Python311/python.exe" -m venv <경로>/venv311
<경로>/venv311/Scripts/python.exe -m pip install numpy==1.26.4 pandas==2.0.3 scikit-learn==1.8.0 joblib==1.5.3
```

---

## 4. 현재 모델 구성

```python
HistGradientBoostingClassifier(
    max_iter=120, learning_rate=0.08, max_leaf_nodes=31,
    min_samples_leaf=200, l2_regularization=10.0, early_stopping=False)
× 8시드 [42, 7, 123, 2024, 99, 555, 31337, 1] 예측 평균
피처 67개 = 원본 47 + 엔지니어링 18 + ★hand delta 2 (2026-08-23 채택)
학습 데이터 2019~2024 전체 1,475,092행
prior = 0.523766 (전체 control_success 평균, 스무딩 사전확률로 모델과 함께 저장)
결측(NaN)은 채우지 않고 GBDT 네이티브 처리에 맡긴다
```

### hand delta (신규 채택 피처, +17.5)
- `f_hand_delta` / `f_bhand_delta` = 투수/타자별 [해당 손 상대 과거 성공률 − 본인 통산 과거 성공률]
- 시즌 단위 as-of (cumsum − 자기 시즌; exp/15에서 토이 실행으로 누수 없음 증명)
- 학습 행: (eid, season, sh) 3키 조인 / test 2025: 전체 train 합계 (eid, sh) 2키 조인
- model.pkl bundle에 `hand_tbl_p`/`hand_tbl_b` 배포 테이블 포함. script.py는 load+join만
- 독립 시드 3회 재현: +13.9 / +17.5 / +22.9

**추론 시간 19.1초** (제한 600초). 시드를 16~24개로 늘려도 시간은 여유 있다.

---

## 5. 확정된 발견 (재실험 불필요)

### 5-1. 노이즈를 넘은 진짜 개선
| 개선 | 크기 | 판정 |
|---|---:|---|
| RandomForest → HistGradientBoosting | **+195** | ✅ |
| G2 매치업 피처 3개 | **+107 / +63** | ✅ |
| 8시드 앙상블 | +24.9 | ✅ |
| **hand delta 2개 (2026-08-23)** | **+17.5** | ✅ 독립 3회 재현 |

G2 = `f_same_hand`(투수손==타자손), `f_scoring_pos`(2·3루 주자), `f_any_runner`

### 5-2. 트리에게 무엇을 만들어줘야 하는가
```
✅ 가치 있음: 트리가 구조적으로 못 하는 계산
     A == B  (비교)  ← f_same_hand 가 +107점
     A − B   (차이)  ← 단, 매치업 정보가 함께 있을 때만

❌ 무가치:    트리가 이미 하는 일
     log(A) 등 단조변환  → 트리는 단조변환에 불변. 같은 행을 나눈다
     결측 플래그         → GBDT가 NaN을 이미 네이티브 처리 (효과 정확히 0.0)
     원본 재조합         → balls*3+strikes 류. 트리가 알아서 조합한다
```

### 5-3. 죽은 축 (더 파지 말 것)
| 축 | 근거 |
|---|---|
| **시즌 드리프트 대응** | 정직한 시도 4종 전부 마이너스(-26~-113). 정답을 훔친 반칙조차 +51점이 천장. GBDT가 season/asof_* 로 이미 94% 자동보정 중 |
| **하이퍼파라미터 용량** | max_iter 120→400 (3배)에 변별력 +6.9 = 노이즈. 시드 앙상블이 이미 용량 부족을 메꾸고 있어서 |
| **베이지안 스무딩 피처 추가** | LOO +3.3 / ADD +4.8 = 노이즈 |
| **볼카운트 파생 피처** | LOO -13.1 / ADD -12.5 = 노이즈 또는 소폭 유해 |
| **★확률 보정 (08-23 확정)** | 실전 곡선은 이미 깨끗함(ECE 0.0106, 상위구간 붕괴는 극단설정 한정). 보정기 모델간 전이 **-150.9 유해**(exp/13). 반칙 천장 +80.4는 합법 경로 없음. HANDOFF 구버전 7-2의 절차는 반증됨 |
| **★XGBoost 혼합 (08-23 확정)** | 연구실 GPU 실측: 최적 가중치 XGB 0.8~1.0에 이득 0.0 — 같은 방식으로 틀림. 단독 성능도 비등(새 시드 +8.2 노이즈). 부산물: XGB 쓸 땐 lossguide+subsample 필수 |
| **★목적함수 (08-23 확정)** | MSE(회귀) vs log loss(분류) 차이 +6.7 = 노이즈. 분류 유지. 둘 평균도 무익 |
| 압박(클러치) 선수별 delta | 보류(기각 아님) — 야구 연구상 불안정 스킬. hand delta가 성공했으므로 카운트 조건부(전략 성향)는 후보로 살아있음 |

### 5-4. 데이터 사실
```
train.csv  1,475,092행 · 2019~2024 6시즌 · 리그 전체(10개 주력팀, 각 9~14%)
           팀 13번만 1.5배 많음 (주최사 연고 구단으로 추정)
test.csv   2025 시즌 · 실제 245,789행 (배포본은 5행 샘플)

시즌별 control_success 평균 r (계속 하락):
  2019 0.5647 → 2020 0.5327 → 2021 0.5328 → 2022 0.5289 → 2023 0.5000 → 2024 0.4861
  · 11개 팀 전부 예외 없이 하락
  · 2023·2024 공통 투수 263명만 봐도 동일하게 하락 (-0.0132)
    → 선수 실력이 아니라 측정/판정 기준 변화
  · 시즌 '안'에서는 방향이 제각각 → 추세가 아니라 연 단위 계단
    → 2025 r은 데이터로 예측 불가

trackman_history.csv  ★ 지문 매칭으로 사용 가능해짐 (2026-08-23, exp/16 시리즈)
  직접 조인은 여전히 불가(ID 교집합 0)지만, 1군 필터 + 좌우 + 월×요일 등판패턴
  + 팀 궤적 + 필링 배정으로 투수 325명 매칭 확정 (train 투구량 71.3% 커버)
  → lab/trackman_match_v3.csv (pid, tid, d, margin, round; round<=2가 엄격 256명)
  확정 사실:
  · 1군 팀코드 11개 (MIN_*=2군, KBO_ARM/POL=상무·경찰, ACE_*=기타 — 제외할 것)
  · 팀 대응: 12=두산 13=LG(주최사 연고 확인) 14=키움 15=롯데 16=KIA 17=한화
             18=삼성 19=NC 20=KT 21=SSG(SK_WYV 통합)
  · 좌우 코딩: train 2=Right, 1=Left
  · trackman은 1군 투구의 ~80%만 커버 (구장별 장비 차, KIA 최소) — 카운트 완전일치 기대 금지
  · asof_pitcher_pitchmix_n == asof_pitcher_n (100% 동일 — trackman 유래 아님)
```

---

## 6. 🚨 대회 규칙 — 위반 시 실격

```
❌ test.csv의 다른 행/전체 분포를 이용한 집계·누적·빈도·rolling·target encoding
❌ 평가 데이터 전체를 보고 만든 사후 보정값
❌ 리더보드 점수로 2025년 평균 r을 역산해서 예측을 이동시키기
   (수학적으로 가능하지만 "전체 평가 데이터의 분포로 보정"에 정확히 해당. 절대 금지)
❌ 외부 데이터 / 원격 API / 허용 범위 밖 사전학습 모델
❌ 2025년 Trackman 데이터, 현재 투구의 실제 측정값·판정·구종
```

**허용**: 확률 보정기를 **학습 데이터**(train.csv 내 연도)로 적합해서
각 테스트 행에 **독립적으로** 적용하는 것은 가능하다 (CLAUDE.md 명시).

### 감사 완료 상태
현재 `submit.zip`의 `script.py`를 전수 검사했고 위반 없음:
- groupby/sort/cumsum/rolling/agg 등 집계 연산 0건
- `preds.mean()`은 print문 안에만 존재. 예측값에 되먹이지 않음
- `acc / len(models)`는 같은 행에 대한 모델 8개 평균 (행끼리 평균 아님)
- import: os, numpy, pandas, joblib 뿐. 네트워크 접근 없음

**새 코드를 짤 때마다 이 감사를 반복하라.**

---

## 7. 진행 중 / 미해결  (※ 7-2 이하 구버전 서술은 08-23 실측으로 대부분 종결 — §5-3 참조)

### ⚡ 7-0. 최신 상황 (08-23 밤) — 다음 세션은 여기부터
```
1. 🚨 hand delta 리더보드 역행: 로컬 +18(3회 재현) → 실제 2025 LB 830.32→808.09(-22.2)
   제출 경로 버그 아님. 가설: 연도 불안정 신호 (2024 한 해로만 검증한 것이 패착)
   → exp/18(2021/22/23 다연도 진단)이 작업 스케줄러 "lg_aimers_exp18"로 분리 실행 중
     결과: lab/18_result.txt (완료 마커 lab/night_done.txt)
   → 830.32짜리 원본 zip은 루트 submit.zip에 보존됨. 진단 전 추가 제출 금지
   → ★ 새 프로토콜: 모든 새 피처는 3개 연도 이상 일관성 통과 후에만 제출
2. exp/17 trackman 피처 1차 = 불발 (엄격 +0.4 무효과, 전체 -15.1 유해) → 축 보류
   매칭 테이블(lab/trackman_match_v3.csv)은 자산으로 유지
3. 남은 살아있는 축: CatBoost+선수ID (OOF 상관 먼저), 카운트 조건부 delta(다연도 검사 필수),
   시드 증량(안전), exp/11 B·C / exp/12 재실행(저우선)
```

### 7-1. 지금 돌고 있을 수 있는 것
```
exp/11_objective_coldstart.py   목적함수 재측정 + cold-start 정량화 + 스무딩 대체
exp/12_leaves.py                max_leaf_nodes 15/31/63/127 비교
연구실(별도 머신)                lab/xgb_gpu.py — XGBoost GPU 비교
```
결과 파일: `%TEMP%\claude\c--Users-gwonn-Desktop-open\<세션>\tasks\*.output`

### 7-2. ⭐ 가장 유망한 미해결 단서 — 신뢰도 곡선 상위 구간 붕괴

exp/10에서 **극단 설정**(2019~2022 학습 → 2024 채점, 2023을 건너뜀)으로 재보니:
```
구간  예측평균  실제평균    차이
 0~7   ...      ...     ±0.018 이내  ✅ 잘 맞음
 8    0.5795   0.5361   +0.0434   ⚠️
 9    0.6907   0.4658   +0.2248   🚨 ← 상위 10%가 완전히 무너짐
```
이 한 구간이 -2,010점을 만들었다 (총점 -1882.3).

**원인 추정**: `asof_pitcher_success_rate`(통산 성공률)가 r=0.56이던 옛 시즌에
오염되어 베테랑을 과대평가한다. 모델이 그걸 믿고 0.69를 예측하지만 실제는 0.47.

**주의**: 이 -1882는 2023을 뺀 과장된 설정의 결과다. 실전 설정(2019~2023 학습)은
같은 알고리즘으로 +700점이 나온다. **메커니즘은 진짜지만 크기는 과장됐다.**

**다음 확인 사항**: exp/11의 A-2 파트가 실전 설정에서 같은 현상을 재는 중.
- 상위 구간이 부풀려져 있으면 → Isotonic 보정으로 실제 점수 확보 가능
- 곡선이 평평하면 → 극단 상황 한정 현상. 무시

**보정을 쓴다면 누수 없는 절차** (규칙상 허용):
```
1. 2019~2023 학습 → 2024 예측 → (예측, 2024정답)으로 Isotonic 적합
2. 2019~2024 학습(최종 모델) → 2025 예측 → 위 보정기 적용
   ※ 보정기는 train.csv 안의 연도로만 적합했으므로 합법
```

### 7-3. 미해결 항목
| 항목 | 상태 |
|---|---|
| MSE(회귀) vs log loss(분류) | exp/03의 -9.5는 **노이즈였다**. exp/11에서 재측정 중 |
| cold-start가 몇 점짜리 문제인가 | exp/11 B파트에서 구간별 '완벽시 이득'으로 정량화 중 |
| XGBoost가 HistGB보다 나은가 | 연구실에서 측정 중 |
| max_leaf_nodes | exp/12에서 측정 중 |

---

## 8. 남은 길 (2026-08-23 갱신 — 보정·XGB혼합·목적함수 축 사망 반영)

1. **trackman '원인측' 피처** (exp/17 진행 중) — 매칭 투수의 릴리스 일관성
   (rel_height/rel_side std = 제구력의 역학 지표), fastball 구속/회전수, extension.
   asof_*는 전부 '결과' 통계라 겹침이 적을 가능성. strict/all 두 매칭 품질 팔로 측정
2. **CatBoost + 선수 ID 범주 직접 투입** — ordered target statistics가 선수 조건부
   통계를 모델 내장으로 생성. HistGB와 정보 경로가 달라 혼합 후보.
   ★ 혼합 전 OOF 오차 상관 ρ부터 측정 (ρ 높으면 중단 — XGB의 교훈)
3. **투수별 카운트 조건부 delta** — 3볼/2스트라이크 접근 전략 성향.
   hand delta와 같은 코드 패턴 (common.py hand_history_table 일반화)
4. **시드 증량 8→16/24** — 추론 19.1초라 여유 충분. 최종 제출 직전 반영
5. 잔여 확인: exp/11 재실행(B cold-start 정량화·C 스무딩대체), exp/12(잎 개수 — 기대 낮음)

피처 아이디어 논의는 별도 세션에서: **FEATURE_BRIEF.md** 참조 (제안 양식 포함)

---

## 9. 파일 지도

```
exp/common.py                    점수함수·로드·피처엔지니어링·인코딩 (실험 공용)
exp/01_signal.py                 시즌구조·드리프트·단순규칙 점수
exp/02_team_check.py             리그전체 데이터 확인·팀별 하락·같은투수 추적
exp/03_drift_and_features.py     드리프트 전략 5종 + 피처효과 + 목적함수
exp/04_ablation.py               피처 그룹 LOO/ADD ablation
exp/05_features_v2.py            가지치기·새 조합 (결과는 전부 노이즈였음)
exp/06_noise_floor.py            ★ 노이즈 바닥 측정 + 시드앙상블 효과
exp/07_train_submit.py           제출용 학습·저장 (venv311로 실행할 것)
exp/08_build_zip.py              zip 생성 + 15종 검증 (가짜 서버 245,789행 실행 포함)
exp/09_tune.py                   하이퍼파라미터 (용량 축 — 죽음. A3 이후 중단)
exp/10_calibration.py            신뢰도 곡선 + shrink/Platt/Isotonic
exp/11_objective_coldstart.py    목적함수(완료: 무차별) + cold-start·스무딩(B·C 재실행 대기)
exp/12_leaves.py                 max_leaf_nodes 비교 (재실행 대기, 기대 낮음)
exp/13_calib_transfer.py         보정기 전이 검증 (완료: -150.9 유해 → 보정 축 사망)
exp/14_hand_delta.py             hand delta 1차 (+13.9)
exp/15_hand_confirm.py           hand delta 확증 (+17.5 채택) + 타자 대칭
exp/16*_trackman_*.py            지문 매칭 4부작 (최종 16d: 325명/71.3%)
exp/17_trackman_feats.py         trackman 원인측 피처 실험
exp/07b_rebuild_dep.py           배포 테이블 엣지 수정용 (일회성, 적용 완료)
lab/xgb_gpu.py                   연구실 GPU용 XGBoost 비교 (완료 — 혼합 축 사망)
lab/trackman_match_v3.csv        ★ 투수 매칭 테이블 (pid↔tid, round<=2=엄격)
FEATURE_BRIEF.md                 피처 아이디어 세션용 브리핑 (별도 세션에서 사용)
submit/                          제출물 원본 (script.py가 피처함수 단일 원본, 67피처)
baseline_submit/                 운영진 베이스라인 (549.51점)
```

### 실행 규칙
```bash
# 백그라운드 실행 시 반드시 두 개 붙일 것
PYTHONIOENCODING=utf-8 py -3.12 -u exp/XX.py 2>&1
#      ↑ 한글 UnicodeEncodeError 방지    ↑ 출력 버퍼링 해제(진행률 표시)
```
Bash 셸에서 `PYTHONIOENCODING` 없이 돌리면 한글 print에서 즉시 죽는다 (cp1252).

---

## 10. 사용자와 일하는 방식

- **한국어로** 설명한다. "무엇을 / 왜 / 다음 행동" 순서
- **혼자 앞서가지 말 것.** 코드를 짜면 먼저 무엇을 왜 하는지 설명하고,
  중요한 결정은 사용자에게 확인받는다. 실행도 사용자가 직접 하고 싶어할 수 있다
- 초보자도 이해할 수 있게. 다만 내용은 정확하게
- **결과를 정직하게 보고한다.** 실패·노이즈·자기 실수를 숨기지 말 것.
  이 프로젝트에서 가장 값진 발견들이 "내 이전 결론이 틀렸다"에서 나왔다
- 노트북 절전이 켜져 있으면 백그라운드 실험이 멈춘다. 장시간 작업 전 안내할 것
```powershell
powercfg /change standby-timeout-ac 0
powercfg /change hibernate-timeout-ac 0
```
